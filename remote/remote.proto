syntax = "proto3";
package remote;
option go_package = "/github.com/hollywood/remote";
// import "actor.proto";

service Remote {
	rpc Receive(stream Message) returns (stream Message) {}
} 

message Message {
	bytes data = 1;
	int32 target = 2;
	int32 sender = 3;
}